# Dev/CI tooling only -- not needed to run the app itself (see requirements.txt
# for that). Installed by both Dockerfile.dev and .github/workflows/build.yml;
# add a tool here once and both stay in sync, instead of editing each place.
pytest
pytest-cov
ruff
mypy
bandit
pip-audit
